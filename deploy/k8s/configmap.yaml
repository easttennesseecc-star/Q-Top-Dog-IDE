apiVersion: v1
kind: ConfigMap
metadata:
  name: qide-app-config
  namespace: topdog-ide
data:
  FRONTEND_URL: "https://topdog-ide.com"
  BACKEND_URL: "https://topdog-ide.com"
  WORKSPACE_PROFILE: "dev"
  ENABLE_HOST_REDIRECT: "true"
  CANONICAL_HOST: "topdog-ide.com"
  ALTERNATE_HOSTS: "www.topdog-ide.com"
  # SEO verification tokens (set real values via sealed secrets or env overrides)
  GOOGLE_SITE_VERIFICATION_TOKEN: ""
  BING_SITE_VERIFICATION_TOKEN: ""
