apiVersion: v1
kind: ConfigMap
metadata:
  name: qide-staging-app-config
  namespace: topdog-staging
  labels:
    environment: staging
data:
  FRONTEND_URL: "https://staging-app.topdog-ide.com"
  BACKEND_URL: "https://staging-app.topdog-ide.com"
  WORKSPACE_PROFILE: "staging"
  # SEO verification tokens (set in staging as needed for pre-verification)
  GOOGLE_SITE_VERIFICATION_TOKEN: ""
  BING_SITE_VERIFICATION_TOKEN: ""
